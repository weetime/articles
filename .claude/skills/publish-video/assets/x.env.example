# 复制到 ~/.config/video-publish/x.env 并填入真实值(chmod 600)
# developer.x.com → App → Keys and tokens
# 注意:User authentication settings 必须先设为 Read and Write,再重新生成 Access Token
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=
